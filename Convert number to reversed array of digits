function digitize(n) {
  //code here
  let str = n.toString();
  let reversedArr = [];
  let arr = str.split('').reverse();
for (let i = 0; i < arr.length; i++) {
reversedArr.push(+arr[i]);
}
return reversedArr;
}
